[package]
name = "lending"
version = "0.1.0"
description = "Lending fixture program with intentional bugs for agent testing"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "lending"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = "0.29.0"
